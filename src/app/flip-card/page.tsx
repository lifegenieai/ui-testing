import CardFlip from "@/components/ui/flip-card";

export default function DemoOne() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gradient-to-br from-slate-50 to-slate-100 dark:from-zinc-950 dark:to-zinc-900 p-8">
      <CardFlip />
    </div>
  );
}
